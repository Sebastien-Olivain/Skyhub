<form method="post" action="{{ path('app_back_session_delete', {'id': session.id}) }}" onsubmit="return confirm('Êtes vous sure de vouloir supprimer cette session?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
